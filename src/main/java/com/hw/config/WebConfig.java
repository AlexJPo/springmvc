package com.hw.config;
/*
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.ComponentScan;
import org.springframework.context.annotation.Configuration;
import org.springframework.controller.servlet.config.annotation.EnableWebMvc;
import org.springframework.controller.servlet.config.annotation.ResourceHandlerRegistry;
import org.springframework.controller.servlet.config.annotation.ViewControllerRegistry;
import org.springframework.controller.servlet.config.annotation.WebMvcConfigurerAdapter;
import org.springframework.controller.servlet.view.InternalResourceViewResolver;
import org.springframework.controller.servlet.view.JstlView;

@Configuration
@EnableWebMvc
@ComponentScan({ "com.hw.controller" })
public class WebConfig extends WebMvcConfigurerAdapter {

    @Bean
    public InternalResourceViewResolver viewResolver() {
        InternalResourceViewResolver viewResolver = new InternalResourceViewResolver();

        viewResolver.setViewClass(JstlView.class);
        viewResolver.setPrefix("/views/jsp/");
        viewResolver.setSuffix(".jsp");

        return viewResolver;
    }

}*/
